spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/reservations_db
    username: reservations_user
    password: reservations_pass
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  liquibase:
    change-log: classpath:liquibase/changelog-master.xml
